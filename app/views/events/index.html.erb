    <header class="jumbotron my-4">
      <h1 class="display-3 text-light mt-5">Events for Everybody!</h1>
      <p class="lead text-light">Organisez des évènements ou rejoignez les évènements près de chez vous</p>
    </header>

    <div class="card-deck mx-3">
    <% @events.all.each do |event| %>
       <div class="col-lg-3 col-md-6 mb-4">
        <div class="card h-100">
          <%= image_tag "#{["event.jpg","event2.jpg","event3.jpg","event4.jpg","event5.jpg"].sample}", class:"card-img-top" %>
          <div class="card-body">
            <h4 class="card-title"><%=event.title%></h4>
            <p class="card-text"><%=event.description%></p>
          </div>
          <div class="card-footer">
            <a href="#" class="btn btn-danger">Inscris-toi!</a>
          </div>
        </div>
      </div>
    <%end%>
	</div>
  </div>
